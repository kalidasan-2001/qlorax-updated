# Training Dependencies for QLORAX
# Use ONLY for training container - excludes UI/dev/notebook tools
# torch/torchvision/torchaudio/triton provided by base image

# Transformers and Hugging Face ecosystem
transformers>=4.35.0,<5.0.0
tokenizers>=0.15.0
accelerate>=0.24.0
datasets>=2.14.0
evaluate>=0.4.0

# Fine-tuning and Parameter Efficient Training
peft>=0.6.0
bitsandbytes>=0.41.0
scipy>=1.11.0
axolotl>=0.12.0,<0.13.0
optimum>=1.16.0

# Core data processing
numpy>=1.24.0,<2.0.0
pandas>=2.1.0

# Configuration and logging
pyyaml>=6.0.0
wandb>=0.16.0
tensorboard>=2.15.0
tqdm>=4.66.0

# ML utilities
scikit-learn>=1.3.0
safetensors>=0.4.0

# System utilities
psutil>=5.9.0
packaging>=23.2
requests>=2.31.0

# REMOVED: instructlab (data generation only - use requirements-instructlab.txt)
# REMOVED: sentence-transformers (evaluation only - use with benchmark.py)
# REMOVED: rouge-score, sacrebleu, bert-score (evaluation metrics - not training)
